# ---- Web / API layer -------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # multipart form parsing -> file uploads
pydantic==2.10.4
pydantic-settings==2.7.0        # typed settings loaded from .env

# ---- UI --------------------------------------------------------------
streamlit==1.41.1
requests==2.32.3                # Streamlit -> FastAPI calls

# ---- Document parsing ------------------------------------------------
pypdf==5.1.0                    # PDF text extraction
python-docx==1.1.2              # .docx
beautifulsoup4==4.12.3          # HTML -> text
lxml==5.3.0                     # parser backend for beautifulsoup
openpyxl==3.1.5                 # xlsx/xlsm; csv and tsv use the stdlib

# ---- Embeddings & vector search --------------------------------------
google-genai==1.16.1            # Gemini embeddings and generation
numpy==2.2.1                    # vector maths; the store is SQLite itself

# ---- Dev / test ------------------------------------------------------
pytest==8.3.4
httpx==0.28.1                   # FastAPI TestClient transport
reportlab==4.2.5                # test-only: generates real PDFs to parse

# NOTE: dependencies for embeddings, the vector store and the LLM provider
# are added in later steps, so that `pip install -r requirements.txt` always
# reflects a working state of the project.
